# --- Data and Pipeline ---
data/                # Ignore all raw rasters (.tif) and datasets
_targets/            # Ignore the targets internal data store
_targets.user        # Ignore local targets user metadata

# --- R Environment ---
.Rproj.user/         # Ignore RStudio project-specific state
.Rhistory            # Ignore your command history
.RData               # Ignore workspace images
.Ruserdata           # Ignore local user data

# --- Quarto and Presentation ---
# We keep the 'docs/' folder because it contains the gh-pages assets
/.quarto/            # Ignore Quarto internal cache
*_cache/             # Ignore computation caches
*_files/             # Ignore temporary rendering files.Rproj.user
.Rproj.user
.Rproj.user
